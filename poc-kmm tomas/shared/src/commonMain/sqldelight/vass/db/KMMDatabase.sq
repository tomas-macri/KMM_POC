CREATE TABLE QuoteEntity(
id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
quote TEXT NOT NULL,
author TEXT NOT NULL
);

getQuotes:
SELECT * FROM QuoteEntity;

insertQuote:
INSERT OR FAIL INTO QuoteEntity (id, quote, author) VALUES (?,?, ?);

resetQuotes:
DELETE FROM QuoteEntity;